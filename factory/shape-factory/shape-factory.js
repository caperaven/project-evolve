/**
 * @class ShapeFactory - this class is responsible for creating the different types of shapes for the game
 *
 * method:
 * - createShape
 *
 * types of shapes: Which can be determined by enum brickType
 * - Standard shape - durablity: 1
 * - Durable shape - durability: 3
 * - Trick shape - durability: 5
 * - Special shape - durability: 7 - instant loss/power ups
 * - Steel shape - durability: 10
 */

class ShapeFactory {
    /**
     * @method createShape - creates a shape based on the shapeType
     */
    static async createShape() {
        //the following function should create a shape based on the shapeType
        return;
    }

}